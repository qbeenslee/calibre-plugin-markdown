# -*- coding: utf-8 -*-
"""Helpers shared by the input and output sides.

  * prefs - the plugin's JSONConfig preferences and the global overrides
  * library - read-only calibre library lookups (book folder and metadata)
  * helpers - calibre-free Markdown export helpers (tables, TOC, YAML, images)
  * override_model - which overrides the customization dialog enabled
  * plugin_icons - the package's own icons for the conversion panes
"""

__license__ = 'GPL 3'
