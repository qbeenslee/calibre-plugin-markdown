# Changelog
记录各个版本发生的变化, 按照:`新增`/`改进`/`修复`记录每一次大版本.

## 3.20.9 (2026-09-26)
### 修复
- `single`（每行一段）样式下不再把块结构周围的空行一起删掉：那些空行不是段落分隔，删掉后 Markdown 会把块前后的文字读成块的一部分——列表后的一段被吞成最后一个条目的 lazy continuation 行、表格后的一段被吞成表格的一行（GFM 规范 Example 202 就是这个行为）、定义列表后的一段被吞进 `<dd>`、引用块结束处的后一行被吞进引用；而 python-markdown（输入插件读 .md 用的同一个解析器）根本不认段落里的列表与表格，所以列表、表格**前**的空行同样保留。列表条目内部的空行（条目第二段那行「空行 + 缩进」）也保留，否则条目两段并成一段；表格上方的 `*标题*` 行与表头之间的空行、脚注定义块前的空行同理。段落之间的空行照旧全部删除，围栏代码块内、引用块结束处这两条既有规则不变，`* * *`、围栏代码块前后、进入引用处仍不补空行（它们本来就能打断段落，`single` 保持紧凑）。实现改为对整份文件逐行分类（`utils/helpers.py` 的 `_classify_lines`：列表项与条目续行、表格行、表格上方标题行、定义列表的术语行与 `: ` 行、脚注定义行，围栏内的行一律按内容），空行两侧任一侧是这些结构就留下，同一处的空行串合并成一个（围栏内的不动）；另有两条：结构下方紧跟引用行时也要留空行（引用打断得了段落，打断不了列表项，那行 `>` 会被读成条目的 lazy continuation 行），引用之后不带 `>` 前缀的正文行仍算引用（引用是读到下一个空行为止，渲染器写引用里的 `<pre>` 时不带前缀），它后面的空行是引用的终止。验收：`scripts/probe_single_style_blank_lines.py` 用 python-markdown 比对 block 版与 `single` 版的渲染结果，24/24 结构案例一致；`scripts/verify_single_style_blank_lines.py` 在 371/394/427/818/17/919 六本真书上比对块结构指纹，6/6 一致；单元测试 505 passed。

## 3.20.8 (2026-09-26)
### 改进
- 输出渲染器按渲染域拆分：`markdownml_enhanced.py` 从 1446 行降到 303 行，只保留类状态、每本书的生命周期与 `dump_text` 分派；列表、引用与图、标题、脚注、表格、链接与图片、目录与文首、行内标记与代码、文本转义九个渲染域各成一个 mixin 模块，放在 `output/renderers/` 下，共享常量与纯函数集中在 `renderers/primitives.py`。60 个方法逐字搬运（AST 比对逐字无差异），生成的 Markdown 与旧版一致，打包与设置面板不受影响。

## 3.20.7 (2026-09-26)
### 修复
- EPUB → MD 时列表项的文字不再掉到子弹下一行：书把条目文字包在 `<p>` 里（`<li>\n<p>大一的林晓晓…</p>\n</li>`），上游把那个 `<p>` 当独立块渲染、自带一个换行，于是输出成 `- ` + 换行 + 文字（条目成了空项，文字掉出列表），书里缩进用的空白还被折成空格，成了 `-  `。现在列表项里的 `<p>`/`<div>` 包裹是透明的（Markdown 的条目本身就是段落），文字直接接在 `- ` 后面；条目里的其他块照旧各占一行（第二个 `<p>` 用空行分段、按条目内容列缩进；引用块渲染成 `- > 引文`；嵌套列表仍在条目下缩进），条目前后用于缩进的空白不再写进行尾。

## 3.20.6 (2026-09-23)
### 修复
- EPUB → MD 时章节标题不再出现层级重复/拆行：`<h2><span>第0001章</span><br/>我成了岳不群</h2>`（书的「转换 HTML」规则还会把内层的章号 `<span>` 改名成 `<h3>`）原来输出 `## ### 第0001章` + 孤立的 `**` 行 + `**我成了岳不群`。现在标题内容一律按行内文本渲染——`<br>` 变空格、块级子元素与嵌套标题压成一行文本（嵌套标题只保留**更深**的那一级，如 `<h2><h3>` 取 `###`）、不写出从标题继承来的 `**`/`*`，链接/图片/sup/sub/code 等合法行内语法照旧。层级本身仍由输入侧与转换规则决定（h1→`#` … h6→`######`），输出插件不做标题判断。

## 3.20.5 (2026-09-23)
### 修复
- MD 转 EPUB 时, 同一张图片被多次引用不再生成多份副本: 内置 TXT Input 对每个 `<img>` 都调用一次 `shift_file`, 而它每次都会另起一个名字(`x.png`、`x-1.png`…), 于是引用 N 次就往 EPUB 里塞 N 份同样的图片。现在相同内容的图片只写一份, 所有引用都指向它(样本《圣女母亲》引用 `images/000001.png` 110 次: 13.1MB / 110 份 → 1.5MB / 1 份)。